#include<bits/stdc++.h>
#define ll long long int
using namespace std;

ll Indegree[10000000];

int main()
{
    ll n,m,i,j;
    while(scanf("%lld %lld",&n,&m) && n && m)
    {
        memset()
        for(i=1;i<=m;i++)
        {
            scanf("%lld %lld",&a,&b);
            Indegree[b]=++;
        }
    }

    return 0;
}

