#include<iostream>
#include<cstdio>
#include<cmath>
#include<map>
using namespace std;
int main()
{
    long long int n,i,j,c;
    while(scanf("%lld",&n)!=EOF)
    {
        c=1;
        for(i=1;i<n;i++)
        {
            c=c*i;
            for(j=2;j<=c;j++)
            {

            }
        }
    }
    return 0;
}
